## 🛡️ Policy Deployment Engine: `google_backup_dr_backup_plan`

This section provides a concise policy evaluation for the `google_backup_dr_backup_plan` resource in GCP.

Reference: [Terraform Registry – google_backup_dr_backup_plan](https://registry.terraform.io/providers/hashicorp/google/latest/docs/resources/google_backup_dr_backup_plan)

---

## Argument Reference  

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `backup_vault` | Required. Backup vault where the backups gets stored using this Backup plan. | true | false | This is a resource reference rather than a generic explicit security control. | None | None |
| `resource_type` | Required. The resource type to which the `BackupPlan` will be applied. Examples include, "compute.googleapis.com/Instance", "compute.googleapis.com/Disk", "sqladmin.googleapis.com/Instance" and "storage.googleapis.com/Bucket". | true | false | his affects compatibility and intended usage, but it is better handled through service-specific validation rather than a explicit security control | None | None |
| `location` | Required. The GCP location for the backup vault should be on approved region. | true | true | Location is a clear security control because it can be enforced through an approved-region whitelist for governance and data residency. | ['australia-southeast1', 'australia-southeast2'] | ['us-central1'] |
| `backup_plan_id` | Required. The ID of the backup plan | true | false |  Plan ID has no impact on the security of the resource or data contained. | None | None |
| `description` | Optional. The description allows for additional details about `BackupPlan` and its use cases to be provided. | false | false | Description has no impact on the security of the resource or data contained. | None | None |
| `max_custom_on_demand_retention_days` | (Optional) The maximum number of days for which an on-demand backup taken with custom retention can be retained. | false | false | Max custom ondemand retention days is a configuration setting and does not directly impact security. | None | None |
| `backup_rules` | Optional. The backup rules for this `BackupPlan`. There must be at least one `BackupRule` message. | false | false | This is a container block. Explicit controls apply to specific fields inside it, not to the block itself. | None | None |
| `log_retention_days` | Optional. This is only applicable for CloudSql resource. Days for which logs will be stored. This value should be greater than or equal to minimum enforced log retention duration of the backup vault. | false | true | Log retention is security-relevant because retained logs support security auditing, monitoring, and incident investigation. A retention period that is too short can reduce visibility and forensic capability. | [30, 90] | [1, 7] |
| `disk_backup_plan_properties` | Optional. Defines optional disk related properties for backups generated by this plan. | false | false | It is a configuration setting and does not directly impact security. | None | None |
| `project` | Optional. The ID of the project in which the resource belongs. | false | false | Project is an administrative scoping field and not a direct explicit security control | None | None |

### backup_rules Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `rule_id` | Required. The unique ID of this `BackupRule`. The `rule_id` is unique per `BackupPlan`. | true | false | This is a identifier only and does not impact security controls for this service. | None | None |
| `backup_retention_days` | Required. Configures the duration for which backup data will be kept. Retention_days must be between 7 and 365 days | true | true | Lower period of retention increases data loss and compliance risk. | [7, 30, 365] | [0, 1] |
| `standard_schedule` | Required. Standard schedule for taking backup. | true | false | Security rules apply to fields inside this block, not the block itself. | None | None |

###   standard_schedule Block

  | Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
  |----------|-------------|----------|-----------------|-----------|-----------|---------------|
  | `recurrence_type` | Required. Configures the duration for which backup data will be kept. Recurrence type should be HOURLY, DAILY or WEEKLY | true | true | Frequency period impacts recovery and compliance. Values outside the approved range increase data loss and compliance risk. | ['HOURLY', 'DAILY', 'WEEKLY'] | ['YEARLY'] |
  | `hourly_frequency` | Optional. Specifies frequency for hourly backups. | false | false | This is conditional and depends on recurrence type, so no security impact by itself. | None | None |
  | `days_of_week` | Optional. Specifies days of week on which jobs will run | false | false | This is conditional schedule logic and is better handled through custom validation | None | None |
  | `days_of_month` | Optional. Specifies days of month on which jobs will run. | false | false | This is conditional schedule logic and is better handled through custom validation | None | None |
  | `week_day_of_month` | Optional. Specifies a weekday of the month on which jobs will run. | false | false | This is conditional schedule logic and is better handled through custom validation | None | None |
  | `months` | Optional. Specifies months on which jobs will run. | false | false | This is conditional schedule logic and is better handled through custom validation | None | None |
  | `time_zone` | Required. The time zone to be used when interpreting the schedule. | true | false | This affects operational timing, but it is not usually enforced as an explicit security control. | None | None |
  | `backup_window` | Optional. Defines the window during which backup jobs will run. | false | false | This is conditional schedule logic and is better handled through custom validation | None | None |

###     backup_window Block

    | Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
    |----------|-------------|----------|-----------------|-----------|-----------|---------------|
    | `start_hour_of_day` | Required within backup_window. The hour of the day (0-23) when the window starts. | true | false | This is an operational scheduling field, not an explicit security control | None | None |
    | `end_hour_of_day` | Required within backup_window. The hour of the day (1-24) when the window ends. | true | false | This is an operational scheduling field, not an explicit security control | None | None |
