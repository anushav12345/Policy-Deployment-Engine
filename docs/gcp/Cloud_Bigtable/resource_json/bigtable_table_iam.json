{
  "resource_name": "bigtable_table_iam",
  "subcategory": "Cloud Bigtable",
  "arguments": {
    "instance_name": {
      "description": "The name or relative resource id of the instance that owns the table.",
      "required": true,
      "security_impact": false,
      "rationale": "Points IAM at the correct instance; it is a resource locator, not a principal identifier and not a secret.",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "table": {
      "description": "The name or relative resource id of the table to manage IAM policies for.",
      "required": true,
      "security_impact": false,
      "rationale": "Selects which table the binding applies to; table names alone do not authorize callers.",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "member/members": {
      "description": "Identities granted the privilege in `role` (for `google_bigtable_table_iam_member` or `google_bigtable_table_iam_binding`). Each entry can be one of: **allUsers**, **allAuthenticatedUsers**, **user:{email}**, **serviceAccount:{email}**, **group:{email}**, **domain:{domain}**.",
      "required": true,
      "security_impact": true,
      "rationale": "Public principals on table IAM expose table data access to broad unaudited audiences.",
      "compliant": "serviceAccount:reader@pde-test-project.iam.gserviceaccount.com",
      "non-compliant": "allUsers",
      "parent": null
    },
    "role": {
      "description": "The role that should be applied. Only one `google_bigtable_table_iam_binding` can be used per role. Custom roles must use `[projects|organizations]/{parent-name}/roles/{role-name}`. See [Bigtable roles](https://cloud.google.com/bigtable/docs/access-control#roles).",
      "required": true,
      "security_impact": false,
      "rationale": "Names a permission bundle; without a `member` it does not grant access—like naming a role without assigning a user.",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "policy_data": {
      "description": "For `google_bigtable_table_iam_policy` only: the policy data generated by a `google_iam_policy` data source.",
      "required": true,
      "security_impact": false,
      "rationale": "Holds the policy document object; the field is a vehicle for the policy JSON—grants are defined by bindings inside the data, not by the parameter name.",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "project": {
      "description": "The project in which the table belongs. If it is not provided, Terraform will use the provider default. **Terraform:** do not combine `google_bigtable_table_iam_policy` with `google_bigtable_table_iam_binding` or `google_bigtable_table_iam_member` for the same table (authoritative policy replaces the whole policy). Binding and member may be used together only when they do not grant the same role.",
      "required": false,
      "security_impact": false,
      "rationale": "Non-secret project scope for the Terraform resource; access follows from `member`/`members` + `role`.",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    }
  }
}
